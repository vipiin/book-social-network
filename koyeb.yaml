type: service
name: book-social-network

services:
  - name: backend
    git:
      repository: https://github.com/vipiin/book-social-network
      branch: main
    build:
      dockerfile: Dockerfile.backend
    env:
      - name: SPRING_PROFILES_ACTIVE
        value: prod
      - name: JDBC_DATABASE_URL
        secret: JDBC_DATABASE_URL
      - name: DB_USERNAME
        secret: DB_USERNAME
      - name: DB_PASSWORD
        secret: DB_PASSWORD
      - name: SPRING_MAIL_HOST
        value: smtp-relay.brevo.com
      - name: SPRING_MAIL_PORT
        value: "587"
      - name: SPRING_MAIL_USERNAME
        secret: BREVO_USER
      - name: SPRING_MAIL_PASSWORD
        secret: BREVO_PASSWORD
      - name: JWT_SECRET
        secret: JWT_SECRET
      - name: CLOUDINARY_CLOUD_NAME
        secret: CLOUDINARY_CLOUD_NAME
      - name: CLOUDINARY_API_KEY
        secret: CLOUDINARY_API_KEY
      - name: CLOUDINARY_API_SECRET
        secret: CLOUDINARY_API_SECRET
      - name: FRONTEND_URL
        value: https://frontend-app.koyeb.app
    ports:
      - port: 8088
        path: /api/v1/health
    routes:
      - path: /api
        port: 8088

  - name: frontend
    git:
      repository: https://github.com/vipiin/book-social-network
      branch: main
    build:
      dockerfile: Dockerfile.frontend
    ports:
      - port: 80
        path: /
    routes:
      - path: /
        port: 80
